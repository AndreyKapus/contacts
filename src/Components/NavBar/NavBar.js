import { SignUpForm } from "../SignUp/SignUp";

export const NavBar = () => {
    return (
        <div>
            <h1>Contacts</h1>
            <button type="button">Sign up</button>
            <SignUpForm/>
        </div>
    )
};
